.DS_Store
.claude/
# large local media in the project root (test/ clips are tracked)
/*.mp4
/*.mov
# local media dropped into test/ — only the generated ground-truth clips are tracked
test/*
!test/frames-30fps.mp4
!test/frames-30fps-audio.mp4
# local media library (docker compose mounts this at /app/videos)
videos/
